import React from 'react'

const {{component}} = props =>  {
	return (
		<div className={'{{component}}'.replace(/([A-Z])/g, (g) => `-${g[0].toLowerCase()}`)}>
			<p>Component {{component}}</p>
		</div>
	)
}

export default {{component}}